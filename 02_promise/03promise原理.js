// promise代表一个异步操作
// promise有三个状态分别为 pending(进行中),fulfilled(已经成功),rejected(已经失败)
// 只有异步操作的结果才可以改变这个状态，其他任何操作都无法改变这个状态
// 如果状态改变后就不能再改变了 状态改变只会有两种 从pending(进行中)-->fulfilled(已经成功)，或者
// 从pending(进行中)-->rejected(已经失败)

